import 'package:flutter/material.dart';

class submittedReservation extends StatelessWidget {
  //const submittedReservation({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          alignment: Alignment.center,
          child: (Text('Hello thank you for using libtech.'))),
    );
  }
}
